pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/alishbalaeeq/FlaskApp.git'
            }
        }
        
        stage('Build') {
            steps {
                echo 'Building'
                bat 'pip3 install -r requirements.txt'
            }
        }
        
        stage('Deploy') {
            steps {
                script {
                    def data =  load 'functions.groovy'
                    def SERVER = "PROD"
                    data.deploy(SERVER)
                }
            }
        }
    }
}
